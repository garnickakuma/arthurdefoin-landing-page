{% layout 'theme' %}

<div style="max-width:680px;margin:120px auto;padding:0 32px;text-align:center;">
  <div style="color:var(--gold-400);font-size:48px;margin-bottom:16px;">✦</div>
  <span class="vx-eyebrow">Page introuvable</span>
  <h1 class="bs-display" style="font-size:62px;margin:16px 0 20px;">404</h1>
  <p style="font-family:var(--font-sans);font-size:17px;line-height:1.7;color:var(--ink-500);margin-bottom:36px;">
    Cette page n'existe pas ou a été déplacée. Explorez notre boutique pour trouver votre bonheur.
  </p>
  <a href="{{ routes.root_url }}" class="btn btn--gold btn--lg">Retour à l'accueil</a>
</div>
